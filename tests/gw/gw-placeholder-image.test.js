
import placeholderImage from '../../src/js/gw/gw-placeholder-image';
/* global describe, it, expect */

describe('Placeholder Image', () => {
  let sut;
  it('should exist', () => {
    sut = placeholderImage;
    expect(sut).toBeTruthy();
  });
});